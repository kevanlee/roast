# Roast Web App

This project bootstraps the Roast web application with a modern frontend toolchain (Vite + vanilla JS). Install dependencies with `npm install` to get started.

## Scripts
- `npm run dev`: Start the development server.
- `npm run build`: Bundle the app for production.
- `npm run preview`: Preview the production build locally.
- `npm run lint`: Lint the source files.

## Project Structure
```
Roast/
├── public/           # Static assets served at root
├── src/              # Application source code
├── README.md
├── package.json
└── .gitignore
```

Feel free to swap in your preferred framework or styling approach.
